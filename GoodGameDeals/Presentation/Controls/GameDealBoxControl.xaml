<UserControl
    x:Name="userControl"
    x:Class="GoodGameDeals.Presentation.Controls.GameDealBoxControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GoodGameDeals.Presentation.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="217"
    d:DesignWidth="187">

    <Grid Width="187" Height="217">
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Image HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            <TextBlock x:Name="GameTitleView" Style="{StaticResource BaseTextBlockStyle}" Text="{Binding GameTitle, ElementName=userControl}"
                   IsHitTestVisible="False"
                   VerticalAlignment="Top" TextTrimming="CharacterEllipsis" Grid.Row="1"/>

    </Grid>
</UserControl>
