<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:isAcrylicBrushNotPresent="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:Controls="using:GoodGameDeals.Controls"
    x:Class="GoodGameDeals.Presentation.Views.MainPage"
    mc:Ignorable="d">

    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition />
        </TransitionCollection>
    </Page.Transitions>

    <Page.DataContext>
        <Binding Path="ViewModelLocatorInstance.MainPageViewModelInstance" Source="{StaticResource Root}" />
    </Page.DataContext>
    <Grid x:Name="RootGrid" Background="#373737">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="AdaptiveStates"
                              CurrentStateChanged="AdaptiveStatesCurrentStateChanged">
                <VisualState x:Name="DefaultState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="640" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MasterColumn.Width" Value="*" />
                        <Setter Target="DetailColumn.Width" Value="0" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="MasterColumn" Width="640"/>
            <ColumnDefinition x:Name="DetailColumn" Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource HeaderTextBlockStyle}" Text="Latest Deals"
                   Foreground="White"
                   FontFamily="Segoe UI"
                   FontWeight="Light" Margin="24,0,0,0" HorizontalAlignment="Left" />
        <Rectangle HorizontalAlignment="Stretch" Height="1" VerticalAlignment="Bottom"
                   Fill="{StaticResource AccentColor}" Margin="24,0,24,0" />
        <ScrollViewer Margin="24,24,0,24" Grid.Row="1" HorizontalAlignment="Left">
            <ItemsControl HorizontalAlignment="Left" VerticalAlignment="Top"
                          ItemsSource="{Binding GamesCollectionView}" Margin="0,0,16,0">
                <ItemsControl.Resources>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0, 0, 0, 17" />
                    </Style>
                </ItemsControl.Resources>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Controls:GameDealControl GameTitle="{Binding GameTitle}" Image="{Binding GameImage}"
                                                  GameSubtitle="{Binding GameSubtitle}" DealsList="{Binding DealsList}"
                                                  MaxHeight="100" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <isAcrylicBrushNotPresent:Rectangle isAcrylicBrushNotPresent:Grid.Column="1" HorizontalAlignment="Left" Height="385" isAcrylicBrushNotPresent:Grid.Row="1" VerticalAlignment="Top" Width="300" Fill="Red" Margin="50,0,0,0"/>
    </Grid>
</Page>